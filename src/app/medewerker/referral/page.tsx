import ReferralClient from "./ReferralClient";

export const metadata = {
  title: "Vrienden werven - TopTalent",
  description: "Verdien extra door vrienden te werven",
};

export default function ReferralPage() {
  return <ReferralClient />;
}
