import HelpClient from "./HelpClient";

export const metadata = {
  title: "Help & Support - TopTalent",
  description: "Veelgestelde vragen en contact",
};

export default function HelpPage() {
  return <HelpClient />;
}
